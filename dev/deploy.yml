config:
  credentials_id: clouduser

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com

command_groups:
  c_group_1:
    - commands:
        - 'uname -a'
        - 'date'
        - 'ls -lrt /usr/local/'
        - 'df -h'
        - 'cd /usr/local/src'
        - 'pwd'
        - 'ls -l'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1
