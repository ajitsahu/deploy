config:
  credentials_id: clouduser

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com

command_groups:
  c_group_1:
    - commands:
        - 'uname -a'
        - 'df -h'
        - 'ls -lrt /usr/local'
        - 'sudo mkdir -p /usr/local/clouduser'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1
