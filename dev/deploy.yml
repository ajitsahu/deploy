config:
  credentials_id: clouduser
  isSudo: true

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com
  r_group_2:
    - name: node02
      host: ajitsahu3c.mylabserver.com

command_groups:
  c_group_1:
    - commands:
        - 'cat /usr/local/src/file1'
    - puts:
        - from: 'test.sh'
          to: '/usr/local/script'
    - commands:
        - 'sh /usr/local/script/test.sh'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1
    - remote_groups:
        - r_group_2
      command_groups:
        - c_group_1
