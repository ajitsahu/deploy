config:
  credentials_id: clouduser

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com
    - name: node02
      host: ajitsahu3c.mylabserver.com

command_groups:
  c_group_1:
    - commands:
        - 'uname -a'
        - 'df -h'
        - 'ls -lrt /usr/local'
        - 'mkdir -p /usr/local/clouduser'
    - scripts:
        - 'test.sh'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1
