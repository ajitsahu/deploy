config:
  credentials_id: clouduser

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com
command_groups:
  c_group_1:
    - commands:
        - 'ls -lrt'
    - gets:
        - from: 'test.sh'
          to: 'test_new.sh'
    - puts:
        - from: 'test.sh'
          to: '.'
    - removes:
        - 'test.sh'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1
