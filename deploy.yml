config:
  credentials_id: clouduser

remote_groups:
  r_group_1:
    - name: node01
      host: ajitsahu4c.mylabserver.com
command_groups:
  c_group_1:
    - commands:
        - 'ls -lrt'
        - 'sh /usr/local/jenkins/test.sh'
  c_group_2:
    - puts:
        - from: 'test.sh'
          into: '/usr/local/jenkins/'
steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_2
        - c_group_1
